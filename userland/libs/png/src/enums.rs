pub enum CompressionMethod {
    Deflate,
}

pub enum FilterMethod {
    Adaptive,
}

pub enum FilterType {
    None,
    Sub,
    Up,
    Average,
    Paeth,
}

pub enum InterlaceMethod {
    None,
    Adam7,
}
